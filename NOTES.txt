Objectif
--------

Moderniser pic2graph, l'utilitaire de conversion d'un fichier `pic` en image propre bien redimensionnée !

Questions
---------

Utiliser l'API C d'ImageMagick, WandMagick C ? (cf. <https://imagemagick.org/MagickWand/>)
	=> Non, cela complexifierait inutilement les choses.

TODO
----

Utiliser `convert -density 1000` pour obtenir les images de meilleure qualité possible.


Notes
-----

Je dois utiliser `dash`, pas bash !
	=> `man dash` pour voir toutes les possibilités de substitution de chaines de caractères, etc.

Reprendre la même syntaxe d'arguments que groff ?
	pic2graph -Tpng fichier.pic -resize 1920x > out.png
	pic2grpah [groff arguments] INFILE [imagemagick arguments] 
	OU
	pic2graph INFILE [imagemagick arguments] --groff-args [groff arguments]
	OU
	pic2graph INFILE OPTIONS
  		où OPTIONS  est traité : soit comme un argument de pic2graph, soit comme un argument d'IM quand il n'est pas compris comme un argument de pic2graph

Prévoir une grande taille de papier de base (A0 ?) pour être sûr de contenir le diagramme en entier
	groff -k -e -p -Tps -P-pa0 ...
